<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div class="alert <%= name == 'alert' ? "yellow" : "green" %>">
      <a class="close" onclick="$('.alert').hide()">x</a>
      <%= content_tag :div, msg, :id => "flash_#{name}" %>
    </div>
  <% end %>
<% end %>
